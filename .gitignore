/.gradle/
/out/
/dist/
/lib/
/build/
/bin/
# docs/ is tracked (do not ignore) so feature specs and planning docs can be committed
*.jar
!gradle/wrapper/gradle-wrapper.jar

# Local downloads (e.g., `gh run download --dir ./artifacts`)
/artifacts/

launch4j.properties
/package/macosx/

/tvrenamer.iml
/.idea/

# Debug file log output (can be written next to the jar/exe or into build outputs during local runs)
/tvrenamer.log
/build/libs/tvrenamer.log

/src/test/java/org/tvrenamer/controller/TestMain.java
/commit.log
/diff.diff
